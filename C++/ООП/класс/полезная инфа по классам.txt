при передаче параметров в конструктор, при создании объекта следует использовать списки инициализации:

class a{

private:
int atention;
string name;

public:
	a(int a, string b): attention(a), name(b) {
/*КОНСТРУКТОР ПО УМОЛЧАНИЮ*/
}
};