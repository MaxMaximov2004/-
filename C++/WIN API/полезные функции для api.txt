s.c_str() - работает для ANCII и UNICODE строк, переводит строки в массив символов(s-строка) 
P.S. Вообще этот метод возвращает указатель на начальный массив символов (учти что класс string и wstring создают массив из библиотеки vector
